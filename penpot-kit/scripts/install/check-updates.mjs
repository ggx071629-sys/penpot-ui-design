#!/usr/bin/env node
// Updates are reviewed explicitly within this workspace.
process.stdout.write("Project-local toolset: use python3 tools/workspace.py verify at the workspace root. Upstream update checks require a separate authorized review.\n");
