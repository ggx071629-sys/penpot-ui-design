# 本项目新任务启动提示词

首次使用先完成[README 中的环境和连接检查](../README.md)。以下为设计任务启动约定；已知字段由助手整理，关键缺口再确认，不要求用户逐项填写。

你正在当前独立 Penpot 设计工作区中工作。
先读取本项目 `AGENTS.md` 与 `penpot-kit/AGENTS.md`；只使用本项目的 Kit 和 `.agents/skills/` 入口。
本次业务：<业务 slug>；资料目录：`business-projects/<slug>/`；Penpot 文件与页面：<链接/标识>；目标：<目标>；已授权对象与操作范围：<范围>；验收条件：<条件>。
第一条 Penpot 工具调用必须是无参数 `high_level_overview`，随后只读核实当前文件，再按 `penpot-kit/skills/penpot-router/SKILL.md` 选择一个主流程。
复用已知信息和同对象、范围、版本的明确批准；只询问实质缺口。保留新 token、逐框、逐区块及高风险操作的明确审批，并继续独立且已授权的工作。不得以一个技能结束代替完整任务完成。
本工作区负责共用 Penpot 工具、组织业务设计资料、执行设计与审阅，以及交付设计成果。将设计及交接材料存入该业务目录；设计审批覆盖本次明确的设计对象、操作范围和提案版本，后续实施与验收由接收项目按自身授权流程处理。
