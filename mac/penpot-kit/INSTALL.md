# 项目内使用与维护

当前工作区直接使用本目录的工具集，无需运行安装器。

技能入口由工作区 `.agents/skills/` 的相对符号链接提供；MCP 使用客户端配置的连接，不将凭据写入项目。

安装、更新、卸载和配置写入的生命周期入口在此工作区保持禁用，不得移除保护后运行。需要维护时，遵循[工作区维护说明](../docs/maintenance.md)，只操作用户授权的范围。

当前工具集校验：在工作区根目录执行 `python3 tools/workspace.py verify`。
